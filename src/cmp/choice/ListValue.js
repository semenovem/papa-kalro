import React, {Component} from 'react';

/**
 * Список значений для выбора
 * @param {[]|{}[]} props.valueLi - либо набор значений, либо объекты { val, id }
 * @param {Function} choice(val[])
 */
class ListValue extends Component {


    render() {
        return (<div></div>)
    }
}

export default ListValue;
